package linkedlist;
// Node serves as Blueprint for creating individual nodes in list
//                ------------------------------------------------
public class Node {
	// in java linkedlist each element is a separate object called Node
	//each Node has 2 parts
	//* Data(contains value that node stores)  &  * next( is a reference[pointer] to next node in the list)
	
	int data; // here the data we mean is integer
	          // Node class defination includes the 'data' field to store the nodes data and the next field to store reference the the next node in the list
	Node next;//next field is a type of Node bcz it needs to point another 'Node' Object
	
	

}
